# Current Task

> Last updated: 2026-04-25

Only one task is active at a time. When this task is done, either replace the content with the next task or leave a short "No active task" note until one is picked.

---

## No active task

The previous task — "Allow employees to create and assign tasks" — was merged into `dev` on 2026-04-24 (squash commit `643c659`, PR #6). See `BACKLOG.md` (Done section) and `DECISIONS_LOG.md` for details.

To start the next task:

1. Pick an item from `BACKLOG.md` (priority order: Blockers → Should-fix → Nice-to-have).
2. Replace this section with the new active task: goal, scope, branch name, file-by-file scope, quality gates, manual smoke tests, and a Definition of Done checklist (use the structure from prior tasks as a template).
3. Move the picked item from `BACKLOG.md` into `In progress` status.
4. Append a `SESSION_LOG.md` entry when the planning round starts.
